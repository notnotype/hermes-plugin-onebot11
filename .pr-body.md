# OneBot 11 接入最小闭环（私聊+群聊 + 消息查询工具 + 权限管理）

Closes #1

## 变更内容

- **插件骨架**：repo 根即插件目录（`plugin.yaml` / `adapter.py` / `__init__.py`），`hermes plugins install notnotype/hermes-plugin-onebot11` 可直接安装。
- **协议逻辑**（`onebot11/` 包，零 Hermes 依赖）：
  - `message.py`：array 消息段解析（文本/图片/@ 提取，CQ 字符串格式显式不支持）
  - `events.py`：OneBot 事件 → 内部归一化 InboundEvent（私聊 chat_id=user_id、群聊 chat_id=group_id）
  - `ws_server.py`：反向 WS 服务端（Bearer 校验、heartbeat 忽略）
  - `http_api.py`：HTTP 发送与动作调用（重试、长文本分块、reply 段、图片下载）
  - `permissions.py`：权限模型——管理员列表 + 会话范围校验（群聊只能查本群；开放模式只放宽管理员门槛，范围校验始终生效）
  - `tools.py`：三个查询工具 handler，群号/QQ 号一律从会话注入（LLM 无法伪造）
- **Hermes 胶水**（`adapter.py`）：`OneBot11Adapter`（connect/disconnect/send/get_chat_info）、私聊 DM_POLICY、群聊昵称前缀、工具权限桥接（经 gateway runner 的 session source 找回可信会话身份）、`register()` 注册平台 + 三个工具。
- **文档体系**（照搬 neuro-book 工作流）：AGENTS.md、CONTRIBUTING.md、PROJECT-STATUS.md、RELEASE.md、docs/permissions.md、docs/research/、docs/tasks/1-onebot11-min-loop/ walkthrough。
- **CI**：GitHub Actions（ruff + pytest）。

## 验证

- 单元测试：协议层 49 passed + adapter 层 12 passed（adapter 测试需 hermes gateway 可导入，CI 环境自动跳过）
- ruff check 全过
- `hermes plugins list` 可见 onebot11-platform 0.1.0
- 与真实 LLBot 的联调在合入后进行（需修改 LLBot 的 ob11 配置：开启 ws-reverse + HTTP）

## 环境变量

| 变量 | 默认 | 说明 |
|---|---|---|
| `ONEBOT11_WS_PORT` | 18880 | 反向 WS 监听端口 |
| `ONEBOT11_ACCESS_TOKEN` | 空 | WS/HTTP 的 Bearer token |
| `ONEBOT11_HTTP_API` | — | QQ 框架 ob11 HTTP 地址（必填） |
| `ONEBOT11_SELF_ID` | — | 机器人 QQ 号（必填） |
| `ONEBOT11_DM_POLICY` | open | open / allowlist / disabled |
| `ONEBOT11_ALLOWED_USERS` | 空 | 私聊白名单 |
| `ONEBOT11_ADMINS` | 空 | 管理员列表（空 = 开放） |
| `ONEBOT11_HOME_CHANNEL` | 空 | cron 投递目标 |
