# Task 149 真实 Bot 验收记录

日期：2026-08-14

## 结论

当前部署可证明 Hermes/OneBot 运行中、OneBot 连接正常、目标群消息进入插件权限检查；但本次真实 QQ 入站未通过，原因是部署实际生效的白名单与用户提供的目标群不一致或部署配置未按预期加载。不能把本次运行写成中文回执、后台 completion 或截图 MEDIA 回传通过。

## 已核对

- Arch Hermes `gateway_state.json`：`gateway_state=running`，`onebot11.state=connected`。
- 目标容器：`hermes-support-support-hermes-1`，状态 `Up 32 hours (healthy)`。
- OneBot API：容器内 `get_status` 返回 `online=true, good=true`；`get_login_info` 返回机器人 QQ `3101482118`。
- 实际容器环境摘要：`ONEBOT11_ALLOWED_GROUPS=942513604`、`ONEBOT11_SELF_ID=3101482118`、`ONEBOT11_WS_HOST=0.0.0.0`、`ONEBOT11_WS_PORT=18880`、`ONEBOT11_REQUIRE_MENTION=true`。
- 实际部署插件 commit：`e05e4b0f25d7be9aef706dde1d16849f06c742a5`，不是当前 Hermes PR #25 的 `a6298213d81616af54d4883aac6618898c7bc06f`；容器内不存在 `skills/repository-research/SKILL.md`，`adapter.py` 也没有 `register_skill`。
- 实际角色配置：普通用户为 QQ 查询只读工具；超级管理员额外拥有撤回、禁言、踢出和全员禁言工具；实际插件 `onebot11/permissions.py` 仍禁止 delegated child 使用 `delegate_task`、`send_message` 和 `cronjob`。

## 真人入站结果

用户报告：目标群 `942513604`，发送时间 `23:07`。

远端审计在 `2026-08-14 23:07:33` 记录：

```text
OneBot11: 群 942513604 不在当前访问策略，拒绝入队
```

目标群队列最新查询结果：`onebot_queue_message=0`、`onebot_queue_trigger=0`。

因此没有产生可验证的：

1. 中文即时回执；
2. Hermes 工具进度摘要；
3. delegated child completion；
4. 两张剧情工作台截图的 OneBot 媒体发送；
5. QQ 中中文回执→进度→completion→图片的顺序证据。

## 额外观察

远端配置文件 `/opt/data/config.yaml` 的 `platforms.onebot11.extra` 没有直接显示白名单字段，但实际容器环境明确注入 `ONEBOT11_ALLOWED_GROUPS=942513604`。同一时段日志对 `942513604` 仍执行拒绝，说明需要在部署侧检查最终配置解析、角色/白名单覆盖来源和容器实际加载的 plugin/config 版本；本次不修改生产配置、不重启容器、不绕过权限。

目标群历史日志中的旧 `access_denied` 记录不能替代本次入站；此前 `1072992996` 的历史 Agent 回复也不能替代本次目标群验收。

## 当前阻塞

- 生产容器仍运行旧插件 commit，未包含 `repository-research` Skill 注册。
- 目标群消息在 adapter access policy 阶段被拒绝，尚未进入队列。
- 因此本轮不能继续验证后台截图命令、MEDIA 回传或消息顺序。

## 解锁条件

部署侧先确认并修正两项，再重新发送真人 QQ 消息：

1. 实际加载的插件更新到包含 `repository-research` Skill 的 commit `a6298213d81616af54d4883aac6618898c7bc06f`，并确认容器内 Skill 文件存在；
2. 在不扩大其它群权限的前提下，确认目标群 `942513604` 进入最终生效的 `allowed_groups`，重启或 reload 后从容器内配置解析结果核对。

重新验收必须使用真人 QQ 客户端；不使用 HTTP API、构造 WS payload、直接写 SQLite 或手工注入队列替代入站证据。

## 部署修复后复核（2026-08-14 23:48）

### 已验证

- **部署输入已固定**：`SUPPORT_PLUGIN_REF=4bc02b10cfe3d87d25b31c543b4c372e735d6d74`，`SUPPORT_REPO_TARGET_REF=94e500ef897398f6bb7357186d89609394dae36f`，`ONEBOT11_ALLOWED_GROUPS=942513604`，`ONEBOT11_SELF_ID=3101482118`，`ONEBOT11_REQUIRE_MENTION=true`。
- **容器已重建**：目标容器 `hermes-support-support-hermes-1` 于 `2026-08-14T15:48:16.65538695Z` 启动，当前 `state=running`、`health=healthy`。
- **实际加载版本已核对**：容器内 OneBot 插件 commit 为 `4bc02b10cfe3d87d25b31c543b4c372e735d6d74`；`/opt/data/plugins/onebot11-platform/skills/repository-research/SKILL.md` 存在；`adapter.py` 含 `register_skill`；NeuroBook checkout 为 `94e500ef897398f6bb7357186d89609394dae36f`，包含 `scripts/deploy/plot-workbench-screenshot.ts`、`scripts/research/plot-workbench-preview.profile.json` 和 `shared/research-run-contract.ts`。
- **最终白名单解析已核对**：`effective_allowed_groups=['942513604']`；目标群判定 `target_allowed=True`；其它群 `1072992996` 判定 `other_group_allowed=False`。没有扩大群权限。
- **OneBot 健康已核对**：`get_status` 为 `online=True good=True`；`get_login_info` 为 `user_id=3101482118`。容器内 Chromium executable 和 `/opt/data/cache/images` 均存在。

### 真人 QQ 验收状态

本轮仍没有取得真实 QQ 客户端入站消息。`xd://browser` relay 尝试返回 `Browser open timed out after 30000ms`；未使用 HTTP API 发送消息、构造 WS payload、直接写 SQLite、手工注入队列或修改生产权限替代真人入站。因而以下项目仍为**未验证**：

1. 中文即时回执；
2. Hermes 工具进度摘要；
3. delegated child completion；
4. `/plot-workbench.preview` 双视口截图经 Hermes `MEDIA:` 发送；
5. QQ 中“中文回执 → 工具进度 → completion → 正文/图片”的顺序。

部署已具备重新验收前置条件，但不能把静态版本核对、OneBot API 健康探针或已有 NeuroBook 离线截图证据写成真人 pipeline 通过。下一步必须由真人 QQ 客户端在群 `942513604` 发送一次带 @ 机器人的验收消息，再按同一时间窗口核对审计、队列、gateway 日志和 QQ 可见顺序。
