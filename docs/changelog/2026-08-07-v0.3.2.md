# v0.3.2（2026-08-07）

OneBot 11 原始需求收口与 PR #8 可交付修复。

- 合并 release、过期恢复、断开结算和管理员 retry 的同群 pending trigger，避免队列因唯一索引冲突永久阻塞。
- 校验 OneBot raw `self_id`，补齐 completion recovery 和真实 reaction 锚点。
- 群摘要优先通过 Hermes `channel_prompt` 临时注入，旧 Hermes 明确退回有界文本模式。
- 统一超级管理员、WS host、角色工具和 LLM trigger allowlist 配置解析。
- 增加跨平台 Hermes 组合验收入口，补充 pending trigger、上下文、self_id 和 reconnect 回归测试。

本版本仍不包含 RAG、向量库、运行时自优化、OneBot 12、语音转写或原始 WS spool。
