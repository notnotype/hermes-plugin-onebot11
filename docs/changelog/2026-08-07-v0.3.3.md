# v0.3.3（2026-08-07）

OneBot 11 PR #8 收口修复。

- 统一 lease phase 恢复合同：主动断开不消耗失败预算，过期 `agent_running` lease 采用有限退避，未知阶段进入 `uncertain`。
- 修复同群 pending trigger 合并、硬触发 retarget、reaction 锚点和人工 retry 无 trigger 时的恢复入口。
- 同实例 reconnect 现在真正停止旧 WS、heartbeat、dispatcher 和 Hermes background task；旧 managed turn 无 binding 时 fail-closed。
- 收紧 roles、super_admins、LLM trigger、home channel 和运行时 extra 配置解析。
- 增加 home cron smoke、malformed WS 诊断、self_id 去重和长消息分块合同。

本版本不包含 RAG、向量库、运行时自优化、OneBot 12、语音转写或原始 WS spool。
